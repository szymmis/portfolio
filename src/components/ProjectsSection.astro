---
import ProjectEntry from "@/components/ProjectEntry.astro";
import { PROJECTS } from "@/data/projects.ts";
import TechPill from "@/components/TechPill.astro";

const usedTechnologies = [...new Set(PROJECTS.flatMap((p) => p.techs))];
---

<div id="projects" class="pt-6 -mt-6">
  <h2 class="mb-4 text-4xl text-primary">My Projects</h2>

  <div class="flex gap-2 flex-wrap">
    {usedTechnologies.map((tech) => <TechPill tech={tech} />)}
  </div>

  <div class="grid mt-4 xl:gap-12 xl:grid-cols-2 xl:gap-y-6">
    {PROJECTS.map((project) => <ProjectEntry project={project} />)}
  </div>
</div>
